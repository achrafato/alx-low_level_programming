#include "main.h"

/**
* _abs - measure the absolute value
* @n: number to be calculated
* Return: return the absolute value of a number.
*/

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
}
