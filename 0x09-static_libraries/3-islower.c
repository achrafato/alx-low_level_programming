/**
* _islower - Check if the char is uppercase or not
*@c: character to be verified
* Return: return 1 or 0.
*/

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
